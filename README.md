# pa-battleground-counties

## Getting started

```sh
git clone https://github.com/chriszs/pa-battleground-counties.git
cd pa-battleground-counties
pipenv install
pipenv shell
python -m ipykernel install --user --name=pa-battleground-counties
jupyter lab
```
